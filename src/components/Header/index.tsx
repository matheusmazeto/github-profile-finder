import React from 'react';
import { GoMarkGithub } from 'react-icons/go';

const Header: React.FC = () => {
  return (
    <div>
      <GoMarkGithub size={20} color="#333" />
    </div>
  );
};

export default Header;
